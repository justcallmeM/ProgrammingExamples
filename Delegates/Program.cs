namespace Delegates
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Calculator.Initiate();
        }

        //A delegate is a type that represents references to methods with a particular parameters list nd return type.
        //A delegate is a type that safely encapsulates a method.

        //Check multicast delegate
    }
}